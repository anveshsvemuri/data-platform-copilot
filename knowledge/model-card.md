# Customer churn model card

The churn model is a deterministic logistic-regression pipeline trained only on synthetic telecom records. Promotion requires ROC-AUC of at least 0.70. It is a portfolio reference and must not be used for real retention decisions without privacy, fairness, calibration, and domain review.

